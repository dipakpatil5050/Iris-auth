<UserControl x:Class="IrisAuth.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignWidth="1100"
             d:DesignHeight="720"
             Background="#F3F6FB">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ================= HEADER ================= -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="User Management"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="#121518"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Style="{StaticResource addButton}"
                    Content="+ Add New User"
                    Command="{Binding AddUserCommand}"/>
        </Grid>

        <!-- ================= SEARCH ================= -->
        <Grid Grid.Row="1" Margin="0,15,0,10">
            <Grid Width="300" HorizontalAlignment="Right">

                <!-- Placeholder (static – no converter, no crash) -->
                <TextBlock Text="Search by username"
                           Margin="15 0"
                           VerticalAlignment="Center"
                           Foreground="#B0B9C6"
                           IsHitTestVisible="False"
                           Panel.ZIndex="1"/>

                <TextBox x:Name="textBoxSearch"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource searchTextbox}"/>

                <Icon:PackIconMaterial Kind="Magnify"
                                       Width="15"
                                       Height="15"
                                       VerticalAlignment="Center"
                                       Margin="0 0 15 0"
                                       Foreground="#B0B9C6"
                                       HorizontalAlignment="Right"/>
            </Grid>
        </Grid>

        <!--                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       ================= DATAGRID ================= -->
  
     <DataGrid Grid.Row="2"
              ItemsSource="{Binding Users}"
              SelectedItem="{Binding SelectedUser}"
              Style="{DynamicResource DataGridStyle1}"
              RowStyle="{DynamicResource DataGridRowStyle1}"
              CellStyle="{DynamicResource DataGridCellStyle1}"
              ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}"
              HeadersVisibility="Column"
              AutoGenerateColumns="False"
              ColumnHeaderHeight="42"
              RowHeaderWidth="0">
                    <DataGrid.Columns>

                        <DataGridTextColumn Header="ID"
                                Binding="{Binding UserId}"
                                Width="60"/>

                        <DataGridTemplateColumn Header="Username" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Username}"
                                   FontWeight="SemiBold"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Group Name"
                                Binding="{Binding GroupName}"
                                Width="*"/>

                        <DataGridCheckBoxColumn Header="Biometric"
                                    Binding="{Binding IsBiometricEnabled}"
                                    ElementStyle="{StaticResource CheckBoxStyle1}"
                                    Width="120"/>

                        <DataGridCheckBoxColumn Header="Blocked"
                                    Binding="{Binding IsBlocked}"
                                    ElementStyle="{StaticResource CheckBoxStyle1}"
                                    Width="120"/>
                <DataGridTemplateColumn Header="Left Iris" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding IrisLeft, Converter={StaticResource ByteArrayToImage}}" Width="90" Height="60" Stretch="Uniform"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Right Iris" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding IrisRight, Converter={StaticResource ByteArrayToImage}}" Width="90" Height="60" Stretch="Uniform"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Actions" Width="140">
                            <DataGridTemplateColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridColumnHeaderStyle1}">
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGridTemplateColumn.HeaderStyle>

                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                                        <Button Style="{StaticResource gridEditButton}"
                                    Margin="0,0,8,0"
                                    ToolTip="Edit"
                                    Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}">
                                            <Icon:PackIconMaterial Kind="PencilOutline" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>

                                        <Button Style="{StaticResource gridEditButton}"
                                    Margin="0,0,8,0"
                                    ToolTip="Enroll Biometric"
                                    Command="{Binding DataContext.EnrollBiometricCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}">
                                            <Icon:PackIconMaterial Kind="Fingerprint" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>

                                        <Button Style="{StaticResource gridEditButton}"
                                    ToolTip="Block"
                                    Command="{Binding DataContext.ToggleBlockCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}">
                                            <Icon:PackIconMaterial Kind="BlockHelper" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>

                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

        <Border Grid.Row="3" 
        HorizontalAlignment="Right" 
        BorderBrush="#DEE4EC" 
        BorderThickness="1" 
        CornerRadius="5" 
        Padding="5" 
        Background="#ffffff" 
        Margin="0,10,10,10">

            <StackPanel Orientation="Horizontal">

                <Button Style="{StaticResource pagingButton}" 
                Command="{Binding PrevPageCommand}">
                    <Icon:PackIconMaterial Kind="ChevronLeft" Style="{StaticResource pagingButtonIcon}"/>
                </Button>

                <Rectangle Width="1" Fill="#DEE4EC" Margin="3,7"/>

                <ItemsControl ItemsSource="{Binding PaginationButtons}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ContentControl>
                                <ContentControl.Style>
                                    <Style TargetType="ContentControl">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <Button Style="{StaticResource pagingButton}"
                                                Content="{Binding Content}"
                                                Command="{Binding Command}"
                                                CommandParameter="{Binding PageNumber}">
                                                  
                                                </Button>
                                            </Setter.Value>
                                        </Setter>

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEllipsis}" Value="True">
                                                <Setter Property="Content">
                                                    <Setter.Value>
                                                        <TextBlock Text="..." 
                                                           Foreground="#6C7682" 
                                                           VerticalAlignment="Center" 
                                                           Margin="10,0" 
                                                           FontSize="15"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Rectangle Width="1" Fill="#DEE4EC" Margin="3,7"/>

                <Button Style="{StaticResource pagingButton}" 
                Command="{Binding NextPageCommand}">
                    <Icon:PackIconMaterial Kind="ChevronRight" Style="{StaticResource pagingButtonIcon}"/>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
