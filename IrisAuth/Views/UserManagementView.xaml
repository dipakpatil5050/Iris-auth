<!--<UserControl x:Class="IrisAuth.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:viewmodels="clr-namespace:IrisAuth.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="1100"
             d:DesignHeight="720"
             Background="#F3F6FB">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        --><!-- ================= HEADER ================= --><!--
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="User Management"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="#121518"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Style="{StaticResource addButton}"
                    Content="+ Add New Member"
                    Command="{Binding AddUserCommand}"/>
        </Grid>

        --><!-- ================= SEARCH ================= --><!--
        <Grid Grid.Row="1" Margin="0,15,0,10">
            --><!--<Grid.ColumnDefinitions>
                <ColumnDefinition Width="790"/>
            </Grid.ColumnDefinitions>-->

            <!--Search Textbox--><!--

            <Grid Width="300" HorizontalAlignment="Right">
                <TextBlock Margin="20 0" VerticalAlignment="Center" Foreground="#B0B9C6" IsHitTestVisible="False"
                        Panel.ZIndex="1" 
                        Visibility="{Binding ElementName=textBoxSearch, Path=Text}" />

                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Name="textBoxSearch" Style="{StaticResource searchTextbox}"/>

                <Icon:PackIconMaterial Kind="Magnify" Width="15" Height="15" VerticalAlignment="Center" Margin="0 0 15 0" Foreground="#B0B9C6" HorizontalAlignment="Right" />
            </Grid>

            --><!-- Search -->
            <!--<Grid Grid.Column="1">
                <TextBlock Text="Search by username" Grid.Row="4" FontSize="13" FontWeight="SemiBold" Margin="0 0 215 0" Foreground="#121518" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <TextBox Tag="Search By Username..." Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource filterTextBox}"/>
            </Grid>--><!--
        </Grid>

        --><!--Seprator--><!--
        <Separator Height="1" Grid.Row="3" Background="#DAE2EA" Margin="0 -1 0 10"/>-->

        <!-- ================= DATAGRID ================= --><!--
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  Style="{DynamicResource DataGridStyle1}"
                  RowStyle="{DynamicResource DataGridRowStyle1}"
                  CellStyle="{DynamicResource DataGridCellStyle1}"
                  ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}"
                  HeadersVisibility="Column"
                  AutoGenerateColumns="False"
                  ColumnHeaderHeight="40"
                  >
            

            <DataGrid.Columns>

                --><!-- ID --><!--
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding UserId}"
                                    Width="60"/>

                --><!-- MEMBER --><!--
                <DataGridTemplateColumn Header="Username" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                                <StackPanel>
                                    <TextBlock Text="{Binding Username}"
                                               FontWeight="SemiBold"/>

                                </StackPanel>
                                
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                --><!-- Group Name --><!--
                <DataGridTextColumn Header="GroupName"
                                    Binding="{Binding GroupName}"
                                    Width="*"/>

                --><!-- IsBiometricEnabled --><!--
                <DataGridTextColumn Header="Biomatric enable"
                                    Binding="{Binding IsBiometricEnabled}"
                                    Width="160"/>

                --><!-- ACTIONS --><!--
                <DataGridTemplateColumn Header="Operations" Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center">

                                <Button Style="{StaticResource gridEditButton}"
                                        ToolTip="Edit"
                                        Command="{Binding DataContext.EditCommand,
                                                  RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}">
                                    <Icon:PackIconMaterial Kind="PencilOutline"
                                                           Style="{StaticResource gridButtonIcon}"/>
                                </Button>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>--><!--
<UserControl x:Class="IrisAuth.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:viewmodels="clr-namespace:IrisAuth.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="1100"
             d:DesignHeight="720"
             Background="#F3F6FB">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        --><!-- ================= HEADER ================= --><!--
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="User Management"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="#121518"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Style="{StaticResource addButton}"
                    Content="+ Add New Member"
                    Command="{Binding AddUserCommand}"/>
        </Grid>

        --><!-- ================= SEARCH ================= --><!--
        <Grid Grid.Row="1" Margin="0,15,0,10">
            <Grid Width="300" HorizontalAlignment="Right">

                --><!-- Placeholder --><!--
                <TextBlock Text="Search by username"
                           Margin="15 0"
                           VerticalAlignment="Center"
                           Foreground="#B0B9C6"
                           IsHitTestVisible="False"
                           Panel.ZIndex="1"
                           Visibility="{Binding ElementName=textBoxSearch,
                                                Path=Text,
                                                Converter={StaticResource TextToVisibilityConverter}}"/>

                <TextBox x:Name="textBoxSearch"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource searchTextbox}"/>

                <Icon:PackIconMaterial Kind="Magnify"
                                       Width="15"
                                       Height="15"
                                       VerticalAlignment="Center"
                                       Margin="0 0 15 0"
                                       Foreground="#B0B9C6"
                                       HorizontalAlignment="Right"/>
            </Grid>
        </Grid>

        --><!-- ================= DATAGRID ================= --><!--
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  Style="{DynamicResource DataGridStyle1}"
                  RowStyle="{DynamicResource DataGridRowStyle1}"
                  CellStyle="{DynamicResource DataGridCellStyle1}"
                  ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}"
                  HeadersVisibility="Column"
                  AutoGenerateColumns="False"
                  ColumnHeaderHeight="42"
                  RowHeaderWidth="0">

            <DataGrid.Columns>

                --><!-- ID --><!--
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding UserId}"
                                    Width="60">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader"
                               BasedOn="{StaticResource DataGridColumnHeaderStyle1}">
                            <Setter Property="Foreground" Value="#111827"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>

                --><!-- USERNAME --><!--
                <DataGridTemplateColumn Header="Username" Width="*">
                    <DataGridTemplateColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader"
                               BasedOn="{StaticResource DataGridColumnHeaderStyle1}">
                            <Setter Property="Foreground" Value="#111827"/>
                        </Style>
                    </DataGridTemplateColumn.HeaderStyle>

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Username}"
                                       FontWeight="SemiBold"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                --><!-- GROUP --><!--
                <DataGridTextColumn Header="Group Name"
                                    Binding="{Binding GroupName}"
                                    Width="*">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader"
                               BasedOn="{StaticResource DataGridColumnHeaderStyle1}">
                            <Setter Property="Foreground" Value="#111827"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>

                --><!-- BIOMETRIC --><!--
                <DataGridTextColumn Header="Biometric Enabled"
                                    Binding="{Binding IsBiometricEnabled}"
                                    Width="160">
                    <DataGridTextColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader"
                               BasedOn="{StaticResource DataGridColumnHeaderStyle1}">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="#111827"/>
                        </Style>
                    </DataGridTextColumn.HeaderStyle>
                </DataGridTextColumn>

                --><!-- ACTIONS --><!--
                <DataGridTemplateColumn Header="Operations" Width="140">
                    <DataGridTemplateColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader"
                               BasedOn="{StaticResource DataGridColumnHeaderStyle1}">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="#111827"/>
                        </Style>
                    </DataGridTemplateColumn.HeaderStyle>

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center">
                                <Button Style="{StaticResource gridEditButton}"
                                        ToolTip="Edit"
                                        Command="{Binding DataContext.EditCommand,
                                                  RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}">
                                    <Icon:PackIconMaterial Kind="PencilOutline"
                                                           Style="{StaticResource gridButtonIcon}"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>-->
<UserControl x:Class="IrisAuth.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignWidth="1100"
             d:DesignHeight="720"
             Background="#F3F6FB">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================= HEADER ================= -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="User Management"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="#121518"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Style="{StaticResource addButton}"
                    Content="+ Add New Member"
                    Command="{Binding AddUserCommand}"/>
        </Grid>

        <!-- ================= SEARCH ================= -->
        <Grid Grid.Row="1" Margin="0,15,0,10">
            <Grid Width="300" HorizontalAlignment="Right">

                <!-- Placeholder (static – no converter, no crash) -->
                <TextBlock Text="Search by username"
                           Margin="15 0"
                           VerticalAlignment="Center"
                           Foreground="#B0B9C6"
                           IsHitTestVisible="False"
                           Panel.ZIndex="1"/>

                <TextBox x:Name="textBoxSearch"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource searchTextbox}"/>

                <Icon:PackIconMaterial Kind="Magnify"
                                       Width="15"
                                       Height="15"
                                       VerticalAlignment="Center"
                                       Margin="0 0 15 0"
                                       Foreground="#B0B9C6"
                                       HorizontalAlignment="Right"/>
            </Grid>
        </Grid>

        <!-- ================= DATAGRID ================= -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  Style="{DynamicResource DataGridStyle1}"
                  RowStyle="{DynamicResource DataGridRowStyle1}"
                  CellStyle="{DynamicResource DataGridCellStyle1}"
                  ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}"
                  HeadersVisibility="Column"
                  AutoGenerateColumns="False"
                  ColumnHeaderHeight="42"
                  RowHeaderWidth="0">
            <DataGrid.Columns>

                <!-- ID -->
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding UserId}"
                                    Width="60"/>

                <!-- USERNAME -->
                <DataGridTemplateColumn Header="Username" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Username}"
                                       FontWeight="SemiBold"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- GROUP -->
                <DataGridTextColumn Header="Group Name"
                                    Binding="{Binding GroupName}"
                                    Width="*"/>

                <!-- BIOMETRIC -->

                <DataGridCheckBoxColumn 
                 Header="Biometric"
                 Binding="{Binding IsBiometricEnabled}"
                 ElementStyle="{StaticResource CheckBoxStyle1}"
                 Width="120"
                  />

                <DataGridCheckBoxColumn 
                    Header="Blocked"
                    Binding="{Binding IsBlocked}"
                    ElementStyle="{StaticResource CheckBoxStyle1}"
                    Width="120"
                 />



                <!-- ACTIONS -->
                
                <DataGridTemplateColumn Header="Actions" Width="140">
                    <DataGridTemplateColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader"
               BasedOn="{StaticResource DataGridColumnHeaderStyle1}">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGridTemplateColumn.HeaderStyle>

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center">

                                <Button Style="{StaticResource gridEditButton}"
                        Margin="0,0,8,0"
                        ToolTip="Edit"
                        Command="{Binding DataContext.EditCommand,
                                  RelativeSource={RelativeSource AncestorType=DataGrid}}"
                        CommandParameter="{Binding}">
                                    <Icon:PackIconMaterial Kind="PencilOutline"
                                           Style="{StaticResource gridButtonIcon}"/>
                                </Button>

                                <Button Style="{StaticResource gridEditButton}"
                        Margin="0,0,8,0"
                        ToolTip="Enroll Biometric"
                        Command="{Binding DataContext.EnrollBiometricCommand,
                                  RelativeSource={RelativeSource AncestorType=DataGrid}}"
                        CommandParameter="{Binding}">
                                    <Icon:PackIconMaterial Kind="Fingerprint"
                                           Style="{StaticResource gridButtonIcon}"/>
                                </Button>

                                <Button Style="{StaticResource gridEditButton}"
                        ToolTip="Block"
                        Command="{Binding DataContext.ToggleBlockCommand,
                                  RelativeSource={RelativeSource AncestorType=DataGrid}}"
                        CommandParameter="{Binding}">
                                    <Icon:PackIconMaterial Kind="BlockHelper"
                                           Style="{StaticResource gridButtonIcon}"/>
                                </Button>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
