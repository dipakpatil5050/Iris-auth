<UserControl x:Class="IrisAuth.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IrisAuth.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<Grid>
        --><!-- Background image fills the entire control while preserving aspect ratio --><!--
        <Image
            Source="/Images/IrisDashboard.png"
            Stretch="UniformToFill"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True"
            AutomationProperties.Name="Dashboard background image" />

        --><!-- Optional dim overlay to improve contrast for the foreground card --><!--
        <Rectangle HorizontalAlignment="Stretch"
           VerticalAlignment="Stretch" />
    </Grid>-->
    
    <Grid>
        <TextBlock Text="Dashboard"
             Foreground="Black"
             FontSize="20"
             FontWeight="SemiBold"
             TextAlignment="Center"
             DockPanel.Dock="Top"/>
    </Grid>
</UserControl>
